// This file centralizes the configuration for the frontend.

// When running the app in a browser during development, the backend is on localhost.
// When running on a mobile device for testing, this should be your computer's local IP address.
// For production, this will be your deployed backend's public URL.
const API_BASE_URL = 'http://localhost:3000';